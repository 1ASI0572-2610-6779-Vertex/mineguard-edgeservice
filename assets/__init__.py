"""assets bounded context — reserved.

Mirrors the MineGuard platform bounded contexts. Not used by the edge service
yet (the edge only authenticates devices via IAM and ingests heart-rate
telemetry via Monitoring), but the package is kept so the edge stays aligned
with the cloud platform's context map.
"""
